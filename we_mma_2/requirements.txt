# WE_MMA_2 依賴套件
# 基本依賴（必須安裝）

# GUI 和影像處理
opencv-python>=4.5.0
numpy>=1.20.0
Pillow>=8.0.0

# 串口通訊
pyserial>=3.5

# ===============================
# MMAction2 依賴（選用）
# ===============================
# 如果不安裝 MMAction2，系統會使用回退模式（基於規則的簡單動作識別）
# 
# 安裝 MMAction2 請按照以下步驟：
#
# 1. 安裝 PyTorch（根據你的系統選擇）:
#    - CPU: pip install torch torchvision
#    - CUDA: 請訪問 https://pytorch.org/get-started/locally/
#
# 2. 安裝 MMCV:
#    pip install -U openmim
#    mim install mmengine
#    mim install mmcv>=2.0.0
#
# 3. 安裝 MMAction2:
#    git clone https://github.com/open-mmlab/mmaction2.git
#    cd mmaction2
#    pip install -v -e .
#
# 4. 下載預訓練模型:
#    mkdir -p checkpoints
#    wget -P checkpoints https://download.openmmlab.com/mmaction/v1.0/skeleton/posec3d/posec3d_k400.pth
# ===============================

# 以下為 MMAction2 的部分依賴（如果手動安裝 MMAction2，這些會自動安裝）
# torch>=1.8.0
# torchvision>=0.9.0
# mmengine>=0.7.0
# mmcv>=2.0.0
