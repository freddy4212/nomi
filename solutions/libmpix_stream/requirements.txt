# MPIX Stream Client Requirements
# Install these packages to use the image viewer and web interface

# Core image processing and display
opencv-python>=4.5.0
numpy>=1.20.0
Pillow>=8.0.0
pyserial>=3.5

# Web interface dependencies
Flask>=2.3.0
Flask-SocketIO>=5.3.0
eventlet>=0.33.0

# QOI format support (optional but recommended)
# pillow-qoi>=0.1.0  # Uncomment for full QOI decoding support

# Installation commands:
# Basic installation:
# pip install opencv-python numpy pillow pyserial flask flask-socketio eventlet

# With QOI support:
# pip install opencv-python numpy pillow pyserial flask flask-socketio eventlet pillow-qoi

# QOI support notes:
# - Without pillow-qoi: Uses built-in simple QOI decoder (basic functionality)
# - With pillow-qoi: Full QOI decoding support (recommended for best experience)
